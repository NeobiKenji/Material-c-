#include<iostream>
#include<cmath>
int main(){
    double x=5;
    double y=10;
    double z;
    double u=3.14;
    //z= std::max(y, x); //For show the larger number
    //z= std::min(y, x); //For show the smallest number
    //z= pow(5, 2); //For find the squere from each number(only can 2 number)
    //z=sqrt(25); //For search the squere from number is it
    //z= abs(100); //For change the negative number to positve number
    //z = round(u); //for find the nearest number from the decimal
    //z= ceil(u); //for change the decimal number to the highest
    //z = floor(u); //for change the decimal to the lower
    std::cout<<z;
    return 0;
}